node_modules/
build/
.cache/
.env
.env.local
.env.*.local
.shopify/
*.tsbuildinfo

# Prisma generated client — regenerated by `prisma generate` on install
# and on every schema change. Never commit.
app/generated/

# Vite's dev server cache
.vite/
