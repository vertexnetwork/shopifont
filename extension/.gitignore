node_modules/
dist/
*.tsbuildinfo

# Generated by build-icons.mjs from source.svg — kept out of git so
# the SVG remains the single source of truth.
public/icons/icon-*.png

# Submission archives produced by `Compress-Archive` — rebuild from
# source instead of committing.
*.zip
